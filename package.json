{
  "name": "hanja-sansungbi",
  "version": "1.0.9",
  "private": true,
  "description": "한자성어 별똥별 Android 앱",
  "scripts": {
    "build": "powershell -ExecutionPolicy Bypass -File scripts/build-capacitor-web.ps1",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
